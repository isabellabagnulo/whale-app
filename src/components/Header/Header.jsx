import { Link } from 'react-router-dom'

import { Search } from '../Search/Search'

import './Header.css'

export const Header = () => {

    return(
        <header>

            <Search />
            
        </header>
        
    )
}